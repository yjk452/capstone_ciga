model:
  gaussian:
    init_args:
      sh_degree: 3
  mlp_cfg:
    use: true
    
  renderer:
    class_path: internal.renderers.ciga_renderer2.CigaRenderer

trainer:
  check_val_every_n_epoch: 1
  max_steps: 1000

data:
  path: /home/jinholee/Ciga/data/dataset/SF/traina20g1.8
  parser:
    class_path: EstimatedDepthBlockColmap
    init_args:
      down_sample_factor: 1.0
